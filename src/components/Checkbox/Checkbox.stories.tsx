import { Meta, StoryObj } from '@storybook/react';

import { CheckBox, CheckboxProps } from '.';

export default {
  title: 'Components/CheckboxProps',
  component: CheckBox,
} as Meta<CheckboxProps>;

export const Default: StoryObj<CheckboxProps> = {};
